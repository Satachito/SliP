'square = '( ! * ! );
'double = '( ! + ! );
'triple = '( ! + ! + ! );

'fns = [ square double triple ];

'compose = '(
	{	'f = !.0;
		'g = !.1;
		`( !.f.g );
	}.$
);

3.( [ square double ].compose );

'map = (
	!.1.#.?.'[
		[]
		[ ( !.1.0.@ ) ( [ @, !.1.cdr ].map ) ]
	]
);

